name: Scrape Oricon with OCR

on:
  schedule:
    - cron: '0 3 * * *'  # Daily at 3 AM UTC
  workflow_dispatch:  # Manual trigger

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            tesseract-ocr \
            tesseract-ocr-jpn \
            chromium-browser \
            chromium-chromedriver
      
      - name: Install Python dependencies
        run: |
          pip install selenium pytesseract pillow webdriver-manager beautifulsoup4 requests
      
      - name: Run Oricon OCR scraper
        run: python scrape_oricon_ocr.py
      
      - name: Commit and push changes
        run: |
          git config user.name "Oricon Scraper Bot"
          git config user.email "bot@github.com"
          git add data.js
          if git diff --quiet && git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update Oricon rankings via OCR - $(date)"
            git push
          fi
